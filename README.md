Code Usage Instructions:

1、Modify the path parameters in Code files to match your dataset path.

2、Run 3-class five-fold cross-validation to test the model's ASD grading performance.

3、Run 2-class LOSO validation to test the model's ASD screening performance.

4、Run baseline model compare to identify the advantages of MAAT-Net.
